pipeline {
    agent {
        docker {
            image 'docker:24.0.5' // Use Docker CLI image
            args '-v /var/run/docker.sock:/var/run/docker.sock' // Mount host Docker socket
        }
    }

    environment {
        DOCKER_HUB_CREDS = credentials('dockerhub-creds') // Jenkins credentials ID for Docker Hub
        DOCKER_IMAGE = "naveenbalagouni/my-python-app"
    }

    stages {
        stage('Checkout Code') {
            steps {
                git credentialsId: 'your-github-credentials-id', branch: 'main', url: 'https://github.com/NaveenBalagouni/ssd-pratice.git'
            }
        }

        stage('Debug Environment') {
            steps {
                sh 'whoami && hostname && docker --version || echo "Docker not found"'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', usernameVariable: 'DOCKER_USERNAME', passwordVariable: 'DOCKER_PASSWORD')]) {
                        sh """
                            echo \$DOCKER_PASSWORD | docker login -u \$DOCKER_USERNAME --password-stdin
                            docker build -t \$DOCKER_IMAGE:latest .
                        """
                    }
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                script {
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', usernameVariable: 'DOCKER_USERNAME', passwordVariable: 'DOCKER_PASSWORD')]) {
                        sh "docker push \$DOCKER_IMAGE:latest"
                    }
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
        success {
            echo '✅ Docker image built and pushed successfully!'
        }
        failure {
            echo '❌ There was an error during the build or push process.'
        }
    }
}
