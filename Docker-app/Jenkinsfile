pipeline {
  agent {
    kubernetes {
      yamlFile 'Docker-app/services/pod.yaml'
    }
  }
  stages {
    stage('Check Docker') {
      steps {
        container('docker') {
          sh 'docker --version'
        }
      }
    }
  }
}
